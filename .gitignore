claude.md
.claude

.DS_Store
node_modules

# Generated by `npm run build` — Netlify builds this at deploy time
# (see netlify.toml [build]), so it doesn't need to live in the repo.
site/dist

COMPATIBILITY_REPORT.md
recommendations.md
skills

test/sanity-ui
